/* eslint-disable no-unused-vars */
const {
    addBookHandler,
    getBooksHandler,
    getBookDetailHandler,
    editBookHandler,
    deleteBookHandler,
} = require('./handler');

const routes = [

];

module.exports = routes;
